# Legacy scripts

Unmaintained scratch and one-off analysis scripts, kept for reference. They
predate the packaged layout: imports and data paths are stale (several point
at directories that no longer exist) and each needs individual revival
before it will run.
